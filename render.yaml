services:
  - type: worker
    name: telegram-bot
    env: python
    region: frankfurt  # или ваш регион: oregon, frankfurt, singapore и т.д.
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"